#include <bits/stdc++.h>

#include <queue>

using namespace std;

int main()
{

    queue<int> q;

    // insert - > at end
    // q.push(1);
    // q.push(2);
    // q.push(3);

    // // access
    // cout << q.front() << endl;
    // cout << q.back() << endl;

    // // delte
    // // q.pop();
    // cout << q.front() << endl;

    // cout << q.size() << endl;

    // cout << q.empty() << endl; // true-> if size() == 0
    // false if size not 0
    // if (q.empty())
    // {
    //     cout << " quque is empty size :" << q.size() << endl;
    // }
    // true -> 1
    // false -> 0

    // queue<int> q2;
    // q2.push(1);
    // q.swap(q2);
    // // before ->   q1{ 1,2,3,4}    q2{5,6,7}
    // // after -> q1{5,6,7}   q2{1, 2,3 4}
    // cout << q.size() << "  " << q2.size() << endl;

    // traverse - > no indexes/ pointers
}